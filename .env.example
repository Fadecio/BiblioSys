# Credenciais públicas do Supabase (seguras para o frontend — protegidas por RLS, ver
# supabase/migrations/011_create_rls_policies.sql). NUNCA coloque a service_role key aqui:
# ela só deve ser usada em ambiente servidor/admin, nunca em código que roda no navegador.
#
# Copie este arquivo para .env (ou .env.local) e preencha com os valores do seu projeto em
# https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
